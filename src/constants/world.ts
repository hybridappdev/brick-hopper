export const WORLD_WIDTH = 2400;
export const WORLD_HEIGHT = 1200;

export const PLATFORM_WIDTH = 320;
export const PLATFORM_HEIGHT = 24;

/** Horizontal padding before camera stops at world edges. */
export const CAMERA_EDGE_PADDING = 0;

/** Inset from world edges for the player body (px). */
export const WORLD_EDGE_INSET = 24;

/** Fall this far below the spawn platform before respawn. */
export const FALL_RESPAWN_OFFSET_PX = 160;

/** Lives per run (campaign or single-level attempt). */
export const LIVES_PER_RUN = 3;
