export const COIN_RADIUS = 14;

/** Extra reach beyond coin + player bounds for forgiving pickup. */
export const COIN_PICKUP_RADIUS = 28;

/** Bonus reach when hopping at slow speed (precision mode). */
export const COIN_PICKUP_SLOW_BONUS = 18;
